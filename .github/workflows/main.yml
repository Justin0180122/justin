name: Run Python
on: 
    push:

jobs:
    build:
        name: run py1
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@V2
        - name: Run python
          run: 
                python3 test.py #打井號註解
